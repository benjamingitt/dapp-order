version: '3.8'
services:
  database:
    container_name: db
    image: postgres:14.2
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: 'dbtest'
      POSTGRES_USER: 'dbuser'
      POSTGRES_PASSWORD: '1234'
